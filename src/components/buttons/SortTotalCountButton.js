import React, { Component } from 'react';
import { Button } from "react-bootstrap";
import 'bootstrap/dist/css/bootstrap.css';

export default class SortTotalCountButton extends Component {
    render() {
        return (
            <Button>Sort by visit count</Button>
        )
    }
}