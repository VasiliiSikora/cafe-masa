import React, { Component } from 'react';
import { Button } from "react-bootstrap";
import 'bootstrap/dist/css/bootstrap.css';

export default class SortTotalSpendButton extends Component {
    render() {
        return (
            <Button>Sort by total spend</Button>
        )
    }
}